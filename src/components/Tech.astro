---
const tech = [
  "ferris",
  "bun",
  "graphql",
  "go",
  "svelte",
  "react",
  "aws",
  "deno",
  "nextjs",
  "elm",
  "vue",
  "firebase",
  "typescript",
  "zig",
  "lighthouse",
  "css",
  "tailwind",
  "js",
];
---

<div class="tech">
  <p>Learn & discuss exciting cutting edge technology</p>
  <div class="grid">
    {tech.map((x) => <img alt={x} src={`/${x}.svg`} />)}
  </div>
</div>

<style>
  p {
    color: grey;
    font-weight: 400;
    padding-top: 2rem;
    padding-bottom: 2rem;
    text-transform: uppercase;
    text-align: center;
  }
  .tech {
    display: flex;
    padding: 1rem 2rem 4rem 2rem;
    flex-direction: column;
    align-items: center;
    background-color: var(--off-white);
  }
  .grid > img {
    width: 100%;
    height: 100%;
    /* filter: grayscale(); */
  }
  .grid {
    margin: auto;
    max-width: var(--max-width);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(6, 50px);
    gap: 3rem 2rem;
  }
  @media (min-width: 700px) {
    .grid {
      grid-template-columns: repeat(6, 1fr);
      grid-template-rows: repeat(3, 50px);
    }
  }
  @media (min-width: 1000px) {
    .grid {
      grid-template-columns: repeat(9, 1fr);
      grid-template-rows: repeat(2, 50px);
    }
  }
</style>
